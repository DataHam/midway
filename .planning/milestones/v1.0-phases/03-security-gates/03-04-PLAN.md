---
phase: 03-security-gates
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - pages/verify-danny.html
  - pages/verify-helen.html
autonomous: false
requirements:
  - GATE-01
  - GATE-02
  - GATE-03
  - GATE-06
  - GATE-07
  - API-01
  - API-02
must_haves:
  truths:
    - Turnstile widget renders on gate pages
    - Token submission calls correct API endpoint
    - Success redirects browser to subdomain
    - Failure shows modal with error message
    - Rate limit error shows modal with retry message
  artifacts:
    - path: pages/verify-danny.html
      provides: "Danny gate page with Turnstile integration"
      min_lines: 150
    - path: pages/verify-helen.html
      provides: "Helen gate page with Turnstile integration"
      min_lines: 150
  key_links:
    - from: "pages/verify-danny.html"
      to: "/api/verify-danny"
      via: "fetch POST in submitVerification"
      pattern: "fetch\\(['\"]/api/verify-danny"
    - from: "pages/verify-helen.html"
      to: "/api/verify-helen"
      via: "fetch POST in submitVerification"
      pattern: "fetch\\(['\"]/api/verify-helen"
---

<objective>
Update gate pages with Turnstile integration, API calls, and error handling UI.

Purpose: Transform placeholder gate pages into fully functional verification pages that display Turnstile widget, submit tokens to API, handle responses, and show error modals.

Output: Two complete gate pages (verify-danny.html, verify-helen.html) with working Turnstile integration and error handling.
</objective>

<execution_context>
@C:/Users/DannyTam-Tham/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/DannyTam-Tham/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-security-gates/03-SECURITY-GATES-CONTEXT.md
@.planning/phases/03-security-gates/03-02-SUMMARY.md
@.planning/phases/03-security-gates/03-03-SUMMARY.md

# Existing Gate Pages
- pages/verify-danny.html: Placeholder with Turnstile widget, submitVerification stub
- pages/verify-helen.html: Placeholder with Turnstile widget, submitVerification stub

# API Endpoints (from Plans 02 & 03)
- POST /api/verify-danny → 302 redirect or JSON error
- POST /api/verify-helen → 302 redirect or JSON error

# User Decisions to Honor
- Sitekey injection: Build-time via `<script>window.TAMTHAM_SITEKEY = "...";</script>`
- Modal dialog for errors (not inline, not toast)
- Error messages with codes: INVALID_CODE, EXPIRED_TOKEN, RATE_LIMITED, SERVICE_UNAVAILABLE
- Redirect URL: Read from ?redirect query parameter (default: subdomain URL)
- Token submission: POST with JSON body { token: string }
</context>

<interfaces>
<!-- Gate page interfaces -->

From pages/verify-danny.html:
```javascript
// Build-time injected sitekey
window.TAMTHAM_SITEKEY = "0x4AAAA..."; // Injected by build script

// Turnstile callback
function submitVerification(token) {
  // POST to /api/verify-danny with { token }
  // Handle 302 redirect or JSON response
  // Show modal on error
}

// Modal display
function showErrorModal(errorCode, message) {
  // Display modal with error code and message
  // Close button dismisses modal, clears widget
}
```

From pages/verify-helen.html:
```javascript
// Same structure as verify-danny.html but:
// - API endpoint: /api/verify-helen
// - Default redirect: https://helen.tamtham.com
// - Page title: "Verify Access — Helen Tam-Tham"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update Danny gate page with API integration</name>
  <files>pages/verify-danny.html</files>
  <action>
Update verify-danny.html with complete Turnstile integration:

1. **Build-time sitekey injection**:
   - Add script before Turnstile widget initialization:
     ```html
     <script>
       window.TAMTHAM_SITEKEY = "BUILD_TIME_SITEKEY"; // Replaced by build script
     </script>
     ```
   - Replace `data-sitekey="PLACEHOLDER"` with `data-sitekey="window.TAMTHAM_SITEKEY"`

2. **Implement submitVerification function**:
   - Accept token parameter from Turnstile callback
   - Get redirect URL from query parameter (default: https://danny.tamtham.com)
   - POST to /api/verify-danny with JSON body: `{ token, sitekey: window.TAMTHAM_SITEKEY }`
   - Handle response:
     - If 302: let browser follow redirect (no JavaScript handling needed)
     - If JSON success: `window.location.href = response.redirectUrl`
     - If JSON failure: call showErrorModal(error, message)

3. **Implement showErrorModal function**:
   - Create modal DOM structure (if not exists):
     - Overlay with semi-transparent background
     - White card container with padding
     - Error icon (red X)
     - Error title: "Verification Failed"
     - Error message (human-readable)
     - Error code (small text below message)
     - Close button ("Try Again")
   - Show modal by adding visible class
   - Close button: hide modal, reset Turnstile widget (`cf-turnstile-reset`)

4. **Error message mapping** (per locked decisions):
   - INVALID_CODE: "The verification code was incorrect. Please try again."
   - EXPIRED_TOKEN: "The verification code has expired. Please start over."
   - RATE_LIMITED: "Too many failed attempts. Please wait before trying again."
   - SERVICE_UNAVAILABLE: "The security service is temporarily unavailable. Please try again later."

5. **Styling**:
   - Modal centered on screen
   - Semi-transparent overlay (rgba(0,0,0,0.5))
   - White card with rounded corners, shadow
   - Red error icon (✕)
   - Primary colors from brand palette
   - Animation: fade in/out (0.3s)

Reference CONTEXT.md for error messages and modal display requirement. Use Tailwind classes for styling.
</action>
<verify>
  <automated>grep -q "window.TAMTHAM_SITEKEY" pages/verify-danny.html && grep -q "fetch.*\/api\/verify-danny" pages/verify-danny.html && grep -q "showErrorModal" pages/verify-danny.html && echo "✓ All required elements present"</automated>
</verify>
<done>
- Build-time sitekey injection added (window.TAMTHAM_SITEKEY)
- submitVerification function implemented with API call
- POST to /api/verify-danny with JSON body
- showErrorModal function implemented with modal UI
- Modal displays error message and error code
- Close button resets Turnstile widget
- All 4 error messages match locked decisions exactly
- Modal styling uses brand colors and animations
</done>
</task>

<task type="auto">
  <name>Task 2: Update Helen gate page with API integration</name>
  <files>pages/verify-helen.html</files>
  <action>
Update verify-helen.html with complete Turnstile integration (identical pattern to Danny):

1. **Build-time sitekey injection**:
   - Add script before Turnstile widget initialization:
     ```html
     <script>
       window.TAMTHAM_SITEKEY = "BUILD_TIME_SITEKEY"; // Replaced by build script
     </script>
     ```
   - Replace `data-sitekey="PLACEHOLDER_SITE_KEY"` with `data-sitekey="window.TAMTHAM_SITEKEY"`

2. **Implement submitVerification function**:
   - Accept token parameter from Turnstile callback
   - Get redirect URL from query parameter (default: https://helen.tamtham.com)
   - POST to /api/verify-helen with JSON body: `{ token, sitekey: window.TAMTHAM_SITEKEY }`
   - Handle response:
     - If 302: let browser follow redirect
     - If JSON success: `window.location.href = response.redirectUrl`
     - If JSON failure: call showErrorModal(error, message)

3. **Implement showErrorModal function**:
   - Same structure as Danny page modal
   - Centered overlay with semi-transparent background
   - White card container with error icon
   - Error title, message, error code
   - Close button ("Try Again")
   - Hide modal, reset Turnstile widget on close

4. **Error message mapping**:
   - Same 4 error messages as Danny page (IDENTICAL)
   - INVALID_CODE, EXPIRED_TOKEN, RATE_LIMITED, SERVICE_UNAVAILABLE

5. **Styling**:
   - Same modal styling as Danny page (consistent UX)
   - Brand colors, animations, Tailwind classes

Key differences from Danny page:
- API endpoint: /api/verify-helen
- Default redirect URL: https://helen.tamtham.com
- Page title: "Verify Access — Helen Tam-Tham"
- Paragraph text mentions "Helen Tam-Tham's contact page"

Reference 03-04 Task 1 for implementation pattern. Copy structure, change endpoint and redirect URL.
</action>
<verify>
  <automated>grep -q "window.TAMTHAM_SITEKEY" pages/verify-helen.html && grep -q "fetch.*\/api\/verify-helen" pages/verify-helen.html && grep -q "showErrorModal" pages/verify-helen.html && echo "✓ All required elements present"</automated>
</verify>
<done>
- Build-time sitekey injection added (window.TAMTHAM_SITEKEY)
- submitVerification function implemented with API call
- POST to /api/verify-helen with JSON body
- showErrorModal function implemented with modal UI
- Modal displays error message and error code
- Close button resets Turnstile widget
- All 4 error messages match locked decisions exactly
- Same modal styling as Danny page (consistent UX)
</done>
</task>

</tasks>

<verification>
Verify gate pages work end-to-end:

<automated>
# Test 1: Danny gate page has all required elements
grep -q "window.TAMTHAM_SITEKEY" pages/verify-danny.html && \
grep -q "fetch.*\/api\/verify-danny" pages/verify-danny.html && \
grep -q "showErrorModal" pages/verify-danny.html && \
echo "✓ Danny gate page complete"

# Test 2: Helen gate page has all required elements
grep -q "window.TAMTHAM_SITEKEY" pages/verify-helen.html && \
grep -q "fetch.*\/api\/verify-helen" pages/verify-helen.html && \
grep -q "showErrorModal" pages/verify-helen.html && \
echo "✓ Helen gate page complete"

# Test 3: Error messages match locked decisions
grep -q "incorrect" pages/verify-danny.html && \
grep -q "expired" pages/verify-danny.html && \
grep -q "failed attempts" pages/verify-danny.html && \
grep -q "unavailable" pages/verify-danny.html && \
echo "✓ Error messages present"
</automated>
</verification>

<success_criteria>
- pages/verify-danny.html has Turnstile widget with build-time sitekey
- pages/verify-danny.html implements submitVerification with API call
- pages/verify-danny.html implements showErrorModal with modal UI
- pages/verify-helen.html has Turnstile widget with build-time sitekey
- pages/verify-helen.html implements submitVerification with API call
- pages/verify-helen.html implements showErrorModal with modal UI
- Modal displays error message and error code
- Close button resets Turnstile widget
- All 4 error messages match locked decisions exactly
- Both pages use consistent modal styling
- Redirect URL configurable via query parameter
</success_criteria>

<output>
After completion, create `.planning/phases/03-security-gates/03-04-SUMMARY.md`
</output>

<user_setup>
- Doppler account setup for secrets management (Turnstile secret key)
- Build script configuration to inject sitekey at build time
- Cloudflare Turnstile sitekey generation (public sitekey + private secret)
</user_setup>
