---
phase: 04-security-seo
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - pages/index.html
  - pages/danny.html
  - pages/helen.html
autonomous: false
requirements:
  - SEO-04
  - SEO-05
user_setup: []

must_haves:
  truths:
    - All pages have semantic HTML landmarks (nav, main, footer)
    - Each page has exactly one h1 heading
    - Heading hierarchy is sequential (h1 → h2 → h3, no skipping levels)
  artifacts:
    - path: pages/index.html
      provides: "Semantic HTML with landmarks and proper heading hierarchy"
      contains: "<nav>, <main>, <footer>, one h1, h2 sections"
    - path: pages/danny.html
      provides: "Semantic HTML with landmarks and proper heading hierarchy"
      contains: "<nav>, <main>, <footer>, one h1, h2 sections"
    - path: pages/helen.html
      provides: "Semantic HTML with landmarks and proper heading hierarchy"
      contains: "<nav>, <main>, <footer>, one h1, h2 sections"
  key_links:
    - from: pages/index.html
      to: Semantic structure
      via: "HTML5 landmarks and heading hierarchy"
      pattern: "<nav>|<main>|<footer>|<h1>|<h2>"
    - from: pages/danny.html
      to: Semantic structure
      via: "HTML5 landmarks and heading hierarchy"
      pattern: "<nav>|<main>|<footer>|<h1>|<h2>"
    - from: pages/helen.html
      to: Semantic structure
      via: "HTML5 landmarks and heading hierarchy"
      pattern: "<nav>|<main>|<footer>|<h1>|<h2>"
---

<objective>
Verify and update semantic HTML structure across all pages for SEO and accessibility compliance.

Purpose: Ensure search engines and assistive technologies can properly interpret page structure through semantic HTML landmarks and heading hierarchy.

Output: Updated index.html, danny.html, helen.html with nav/main/footer landmarks and proper h1→h2→h3 heading hierarchy.
</objective>

<execution_context>
@C:/Users/DannyTam-Tham/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/DannyTam-Tham/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-security-seo/04-SECURITY-SEO-RESEARCH.md

# Existing context from Phase 1-3
@.planning/phases/02-content-pages/02-content-pages-01-SUMMARY.md
@.planning/phases/02-content-pages/02-content-pages-02-SUMMARY.md
@.planning/phases/02-content-pages/02-content-pages-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit existing HTML pages for semantic structure</name>
  <files>pages/index.html, pages/danny.html, pages/helen.html</files>
  <action>
    Review each HTML page and identify semantic HTML gaps:

    **Checklist for each page:**
    1. **Landmarks present:**
       - `<nav>` element for navigation
       - `<main>` element for primary content
       - `<footer>` element for footer content
       - `<header>` element (optional but recommended for page header)

    2. **Heading hierarchy:**
       - Exactly one `<h1>` per page (page title)
       - Sequential heading levels (h1 → h2 → h3, no skipping)
       - No `<h2>` before `<h1>`
       - Section content uses appropriate heading levels

    3. **ARIA labels (if needed):**
       - `<nav>` with `aria-label` attribute
       - Landmarks with `aria-label` if multiple instances

    **Current state (from Phase 2 summaries):**
    - index.html: Hero section, navigation grid — needs semantic review
    - danny.html: Bio content, consulting services — needs semantic review
    - helen.html: Bio content, research section — needs semantic review

    **Action:**
    - Read each file and document gaps
    - Note which landmarks are missing
    - Note heading hierarchy violations
    - Prepare updates in next task
  </action>
  <verify>
    <automated>grep -E "(<nav|<main|<footer|<header)" pages/*.html</automated>
    <automated>grep -E "<h[1-6]" pages/*.html</automated>
    <manual>
      <step>Open pages/index.html in editor</step>
      <step>Check for nav, main, footer landmarks</step>
      <step>Verify heading hierarchy (h1 → h2 → h3)</step>
      <step>Repeat for danny.html and helen.html</step>
    </manual>
  </verify>
  <done>
    - All 3 pages reviewed for semantic structure
    - Gaps documented (missing landmarks, heading violations)
    - Ready to implement fixes in Task 2
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add semantic HTML landmarks and fix heading hierarchy</name>
  <files>pages/index.html, pages/danny.html, pages/helen.html</files>
  <behavior>
    - index.html: Has nav, main, footer landmarks; one h1; h2 for sections (hero, navigation)
    - danny.html: Has nav, main, footer landmarks; one h1; h2 for sections (bio, services, cv)
    - helen.html: Has nav, main, footer landmarks; one h1; h2 for sections (bio, research, cv)
    - All pages: No heading level skipping; sequential h1 → h2 → h3
    - All pages: nav elements have aria-label attributes
  </behavior>
  <action>
    Update each HTML page with semantic HTML fixes:

    **For index.html:**
    1. Wrap navigation in `<nav aria-label="Main navigation">`
    2. Wrap hero and main content in `<main>`
    3. Add `<footer>` with copyright/contact info
    4. Ensure single `<h1>` for page title (e.g., "Tam-Tham")
    5. Use `<h2>` for section headings (Hero, Navigation cards)
    6. No `<h3>` needed unless nested sections

    **For danny.html:**
    1. Wrap navigation in `<nav aria-label="Main navigation">`
    2. Wrap bio, services, CV content in `<main>`
    3. Add `<footer>` with copyright/contact info
    4. Single `<h1>` for "Danny Tam-Tham" or role title
    5. `<h2>` for sections: "Professional Profile", "Credentials", "Consulting Services", "Download CV"
    6. `<h3>` for subsections within Consulting Services (IT Strategy, Digital Transformation, Healthcare IT)

    **For helen.html:**
    1. Wrap navigation in `<nav aria-label="Main navigation">`
    2. Wrap bio, research, CV content in `<main>`
    3. Add `<footer>` with copyright/contact info
    4. Single `<h1>` for "Helen Tam-Tham" or role title
    5. `<h2>` for sections: "Professional Profile", "Credentials", "Selected Research", "Download CV"
    6. `<h3>` for subsections within Selected Research (if any)

    **Reference:** 04-SECURITY-SEO-RESEARCH.md, Common Pitfalls section (Pitfall 5: Heading Hierarchy Violations)
  </action>
  <verify>
    <automated>grep -E "<h1>" pages/*.html | wc -l</automated>
    <automated>grep -E "<h2>" pages/*.html | wc -l</automated>
    <automated>grep -E "<nav|<main|<footer" pages/*.html | wc -l</automated>
    <automated>grep -r "aria-label.*navigation" pages/*.html</automated>
  </verify>
  <done>
    - All 3 pages have nav, main, footer landmarks
    - Each page has exactly one h1
    - Heading hierarchy sequential (no h2 before h1, no skipping levels)
    - nav elements have aria-label="Main navigation"
    - Sections use h2, subsections use h3 (if applicable)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify semantic HTML structure</name>
  <files>pages/index.html, pages/danny.html, pages/helen.html</files>
  <action>
    Human verification of semantic HTML structure after Tasks 1-2 completion.
    This checkpoint pauses execution for user confirmation before proceeding.
  </action>
  <verify>
    <automated>MISSING — Manual verification required</automated>
  </verify>
  <done>
    - User confirmed semantic HTML correct via manual verification
    - All pages have nav, main, footer landmarks
    - Heading hierarchy validated (h1 → h2 → h3 sequential)
    - Lighthouse accessibility audit passes
  </done>
  <what-built>
    Semantic HTML landmarks (nav, main, footer) added to all pages
    Heading hierarchy corrected (h1 → h2 → h3 sequential)
    ARIA labels added to navigation elements
  </what-built>
  <how-to-verify>
    <step>1. Open https://tamtham.com in browser</step>
    <step>2. View page source (Ctrl+U or Cmd+U)</step>
    <step>3. Search for &lt;nav&gt;, &lt;main&gt;, &lt;footer&gt; — verify all present</step>
    <step>4. Verify exactly one &lt;h1&gt; per page</step>
    <step>5. Check heading order: h1 → h2 → h3 (no skipping)</step>
    <step>6. Repeat for https://tamtham.com/pages/danny.html</step>
    <step>7. Repeat for https://tamtham.com/pages/helen.html</step>
    <step>8. Run Lighthouse audit (DevTools → Lighthouse tab → Accessibility)</step>
    <step>9. Verify "Heading order" and "Proper heading structure" pass</step>
  </how-to-verify>
  <resume-signal>Type "approved" if semantic HTML correct, or describe issues</resume-signal>
</task>

</tasks>

<verification>
**Automated Semantic HTML Checks:**

```bash
# Count h1 tags (should be exactly 3 total, 1 per page)
grep -E "<h1>" pages/*.html | wc -l

# Count landmarks (should be 9 total: 3 nav + 3 main + 3 footer)
grep -E "<nav|<main|<footer" pages/*.html | wc -l

# Check ARIA labels on nav
grep -r "aria-label.*navigation" pages/*.html

# Check heading hierarchy using HTML validation
npm install -g htmlhint && htmlhint pages/*.html
```

**Lighthouse Accessibility Audit:**
```bash
# Install Lighthouse CLI
npm install -g lighthouse

# Run audit on each page
lighthouse https://tamtham.com --only-categories=accessibility --view
lighthouse https://tamtham.com/pages/danny.html --only-categories=accessibility --view
lighthouse https://tamtham.com/pages/helen.html --only-categories=accessibility --view

# Check for "heading-order" and "heading-has-content" warnings
```

**Manual Verification:**
1. View page source for each page
2. Verify nav, main, footer landmarks present
3. Count h1 tags (should be exactly 1 per page)
4. Check heading sequence: h1 → h2 → h3 (no skipping)
5. Verify nav has aria-label attribute
6. Run Lighthouse accessibility audit
7. Confirm "Proper heading structure" passes
</verification>

<success_criteria>
1. All 3 pages (index.html, danny.html, helen.html) have nav, main, footer landmarks
2. Each page has exactly one h1 heading
3. Heading hierarchy is sequential (h1 → h2 → h3, no skipping levels)
4. nav elements have aria-label="Main navigation" attribute
5. No h2 elements before h1 on any page
6. Lighthouse accessibility audit passes "Proper heading structure" check
7. Semantic HTML improves accessibility score (no heading-order warnings)
</success_criteria>

<output>
After completion, create `.planning/phases/04-security-seo/04-03-PLAN-SUMMARY.md`
</output>
