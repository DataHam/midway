---
phase: 04-security-seo
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - pages/danny.html
autonomous: true
requirements:
  - SEO-05
gap_closure: true

must_haves:
  truths:
    - Heading hierarchy is sequential (h1 → h2 → h3, no skipping levels)
  artifacts:
    - path: pages/danny.html
      provides: "Corrected heading hierarchy"
      contains: "No h4 before h2; sidebar uses h3 or styled p element"
  key_links:
    - from: pages/danny.html
      to: SEO-05 requirement
      via: "heading hierarchy fix"
      pattern: "(?<!<h3|<h2>)<h4>"
---

<objective>
Fix heading hierarchy violation in danny.html sidebar.

Purpose: Ensure SEO and accessibility compliance by maintaining sequential heading hierarchy (h1 → h2 → h3) without skipping levels.

Output: Updated danny.html with corrected sidebar heading.
</objective>

<execution_context>
@C:/Users/DannyTam-Tham/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/DannyTam-Tham/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-security-seo/04-SECURITY-SEO-RESEARCH.md
@.planning/phases/04-security-seo/04-SECURITY-SEO-VERIFICATION.md

# Gap closure context
Gap: Heading hierarchy violation in danny.html (line 158 uses h4 before h2 section)
Required fix: Change sidebar h4 to h3 or styled p element
</context>

<tasks>

<task type="auto">
  <name>Fix danny.html heading hierarchy</name>
  <files>pages/danny.html</files>
  <action>
    Fix heading hierarchy violation in danny.html sidebar:

    **Current issue:** Line 158 uses `<h4 class="font-semibold text-primary-navy mb-2">Consulting Services</h4>` in sidebar, but main content section at line 180 uses `<h2>` for the same heading, creating hierarchy violation (h2 → h4 skips h3).

    **Fix:** Change sidebar "Consulting Services" heading from h4 to h3 to maintain sequential hierarchy (h1 → h2 → h3).

    **Action:**
    1. Open pages/danny.html
    2. Find line 158: `<h4 class="font-semibold text-primary-navy mb-2">Consulting Services</h4>`
    3. Change to: `<h3 class="font-semibold text-primary-navy mb-2">Consulting Services</h3>`
    4. Save file

    **Why h3 (not p element):** Keeps semantic meaning as a heading while maintaining hierarchy. The h3 level is appropriate for sidebar subsections under the main h2 sections.

    **Reference:** Gap from 04-SECURITY-SEO-VERIFICATION.md, SEO-05 requirement
  </action>
  <verify>
    <automated>grep -n "Consulting Services" pages/danny.html</automated>
    <automated>grep -c "<h4" pages/danny.html</automated>
  </verify>
  <done>
    - Sidebar "Consulting Services" heading changed from h4 to h3
    - No h4 elements appear before h2 sections in document flow
    - Heading hierarchy follows h1 → h2 → h3 sequence
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify heading hierarchy fix</name>
  <files>pages/danny.html</files>
  <action>
    Human verification of heading hierarchy fix after completion.
  </action>
  <verify>
    <automated>MISSING — Manual verification required</automated>
  </verify>
  <done>
    - User confirmed heading hierarchy correct via manual verification
    - No h4 before h2 violation exists
    - Sequential h1 → h2 → h3 hierarchy maintained
  </done>
  <what-built>
    Fixed heading hierarchy violation in danny.html sidebar
    Changed h4 to h3 for "Consulting Services" in sidebar
  </what-built>
  <how-to-verify>
    <step>1. Open pages/danny.html in editor</step>
    <step>2. Search for "Consulting Services"</step>
    <step>3. Verify sidebar uses &lt;h3&gt; (not &lt;h4&gt;)</step>
    <step>4. View page source in browser</step>
    <step>5. Check heading order: h1 (page title) → h2 (sections) → h3 (sidebar subsections)</step>
    <step>6. Verify no heading level skipping</step>
  </how-to-verify>
  <resume-signal>Type "approved" if heading hierarchy correct, or describe issues</resume-signal>
</task>

</tasks>

<verification>
**Automated Heading Hierarchy Checks:**

```bash
# Count h4 elements (should be 0 or only in valid h3→h4 context)
grep -c "<h4" pages/danny.html

# Verify no h4 before h2 in document flow
grep -n "<h[1-6]" pages/danny.html | awk -F: '{print $2}' | grep -E "<h[1-6]"

# Check semantic structure
grep -E "<nav|<main|<footer" pages/danny.html
```

**Manual Verification:**
1. Open pages/danny.html in browser
2. View page source
3. Check heading sequence: h1 → h2 → h3 (no skipping)
4. Verify sidebar "Consulting Services" uses h3 (not h4)
</verification>

<success_criteria>
1. Sidebar "Consulting Services" heading uses h3 (not h4)
2. No h4 elements appear before h2 sections in document flow
3. Heading hierarchy follows h1 → h2 → h3 sequence
4. SEO-05 requirement satisfied: Proper heading hierarchy
</success_criteria>

<output>
After completion, create `.planning/phases/04-security-seo/04-04-PLAN-SUMMARY.md`
</output>
