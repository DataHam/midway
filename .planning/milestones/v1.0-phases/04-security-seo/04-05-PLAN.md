---
phase: 04-security-seo
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - pages/danny.html
autonomous: true
requirements:
  - SEO-05
gap_closure: true

must_haves:
  truths:
    - Heading hierarchy is sequential (h1 → h2 → h3, no skipping levels)
  artifacts:
    - path: pages/danny.html
      provides: "Corrected heading hierarchy"
      contains: "No h3 before h2; sidebar uses p element instead of heading"
  key_links:
    - from: pages/danny.html
      to: SEO-05 requirement
      via: "sidebar heading fix"
      pattern: "line 158.*<p.*Consulting Services"
---

<objective>
Fix heading hierarchy violation in danny.html sidebar by removing heading element.

Purpose: Ensure SEO and accessibility compliance by maintaining sequential heading hierarchy (h1 → h2 → h3) without any heading appearing before h2 sections.

Output: Updated danny.html with sidebar "Consulting Services" as styled paragraph instead of heading.
</objective>

<execution_context>
@C:/Users/DannyTam-Tham/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/DannyTam-Tham/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-security-seo/04-SECURITY-SEO-RESEARCH.md
@.planning/phases/04-security-seo/04-SECURITY-SEO-VERIFICATION.md

# Gap closure context
Gap: Heading hierarchy violation in danny.html (sidebar h3 before main h2)
Previous fix: Changed h4 to h3 — still violated hierarchy (h3 before h2)
Required fix: Change sidebar heading to p element (non-heading)
</context>

<tasks>

<task type="auto">
  <name>Fix danny.html sidebar heading</name>
  <files>pages/danny.html</files>
  <action>
    Fix heading hierarchy violation in danny.html sidebar:

    **Current issue:** Line 158 uses `<h3 class="font-semibold text-primary-navy mb-2">Consulting Services</h3>` in sidebar BEFORE line 180 uses `<h2>` for the main section, creating hierarchy violation (h3 before h2).

    **Previous fix attempt:** Changed from h4 to h3 — still violated hierarchy because h3 appears before h2 in document flow.

    **Correct fix:** Change sidebar "Consulting Services" from `<h3>` to `<p class="font-semibold text-primary-navy mb-2">Consulting Services</p>` — remove heading element entirely while preserving visual styling.

    **Action:**
    1. Open pages/danny.html
    2. Find line 158: `<h3 class="font-semibold text-primary-navy mb-2">Consulting Services</h3>`
    3. Change to: `<p class="font-semibold text-primary-navy mb-2">Consulting Services</p>`
    4. Save file

    **Why p element (not h3):** The sidebar "Consulting Services" is a subsection label, not a document section heading. Using a styled paragraph preserves visual appearance while maintaining proper heading hierarchy (h1 → h2 → h3 only for actual content sections).

    **Reference:** Gap from 04-SECURITY-SEO-VERIFICATION.md, SEO-05 requirement
  </action>
  <verify>
    <automated>grep -n "Consulting Services" pages/danny.html</automated>
    <automated>grep -c "<h3" pages/danny.html</automated>
  </verify>
  <done>
    - Sidebar "Consulting Services" changed from h3 to styled p element
    - No heading elements appear before h2 sections in document flow
    - Heading hierarchy follows h1 → h2 → h3 sequence
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify heading hierarchy fix</name>
  <files>pages/danny.html</files>
  <action>
    Human verification of heading hierarchy fix after completion.
  </action>
  <verify>
    <automated>MISSING — Manual verification required</automated>
  </verify>
  <done>
    - User confirmed heading hierarchy correct via manual verification
    - No heading before h2 violation exists
    - Sequential h1 → h2 → h3 hierarchy maintained
  </done>
  <what-built>
    Fixed heading hierarchy violation in danny.html sidebar
    Changed sidebar "Consulting Services" from h3 to styled p element
  </what-built>
  <how-to-verify>
    <step>1. Open pages/danny.html in editor</step>
    <step>2. Search for "Consulting Services"</step>
    <step>3. Verify sidebar uses &lt;p&gt; (not &lt;h3&gt;)</step>
    <step>4. View page source in browser</step>
    <step>5. Check heading order: h1 (page title) → h2 (sections) → h3 (content subsections)</step>
    <step>6. Verify no heading level skipping</step>
    <step>7. Verify sidebar "Consulting Services" is styled as bold text but not a heading</step>
  </how-to-verify>
  <resume-signal>Type "approved" if heading hierarchy correct, or describe issues</resume-signal>
</task>

</tasks>

<verification>
**Automated Heading Hierarchy Checks:**

```bash
# Check heading sequence in danny.html
grep -n "<h[1-6]" pages/danny.html

# Verify no h3 before h2 in document flow
grep -n "<h[1-6]" pages/danny.html | awk -F: '{print $1, $2}' | sort -n

# Verify sidebar uses p element
grep -n "Consulting Services" pages/danny.html
```

**Manual Verification:**
1. Open pages/danny.html in browser
2. View page source
3. Check heading sequence: h1 → h2 → h3 (no skipping)
4. Verify sidebar "Consulting Services" uses p element (not h3)
5. Verify visual appearance preserved (bold text)
</verification>

<success_criteria>
1. Sidebar "Consulting Services" uses styled p element (not h3)
2. No heading elements appear before h2 sections in document flow
3. Heading hierarchy follows h1 → h2 → h3 sequence
4. SEO-05 requirement satisfied: Proper heading hierarchy
</success_criteria>

<output>
After completion, create `.planning/phases/04-security-seo/04-05-PLAN-SUMMARY.md`
</output>
