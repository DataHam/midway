---
phase: 05-deployment-infrastructure
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - tests/05-deployment/verify-deployment.sh
  - .planning/phases/05-deployment-infrastructure/WAVE0-CHECKLIST.md
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04
  - DEPLOY-05
  - CLOUD-01
  - CLOUD-02
  - CLOUD-03
  - CLOUD-04
  - CLOUD-05
user_setup: []

must_haves:
  truths:
    - "All 10 deployment requirements verifiable"
    - "Verification script tests all endpoints"
    - "Checklist confirms manual setup complete"
  artifacts:
    - path: tests/05-deployment/verify-deployment.sh
      provides: "Automated deployment verification"
      exports: ["check_azure_swa", "check_cloudflare_dns", "check_tunnels"]
      min_lines: 30
    - path: .planning/phases/05-deployment-infrastructure/WAVE0-CHECKLIST.md
      provides: "Manual setup checklist"
      contains: "All prerequisites verified"
  key_links:
    - from: tests/05-deployment/verify-deployment.sh
      to: DEPLOY-01..05
      via: "curl tests of Azure SWA endpoints"
      pattern: "curl.*azurestaticapps.net"
    - from: tests/05-deployment/verify-deployment.sh
      to: CLOUD-01..05
      via: "curl tests of Cloudflare endpoints"
      pattern: "curl.*tamtham.com"

---

## Phase 05-03: Production Verification & Testing

### Objective

Verify complete deployment pipeline and Cloudflare infrastructure through automated tests and manual checkpoints.

**Purpose:** Ensure all deployment and infrastructure requirements are met before marking phase complete.

**Output:** Verification script, checklist, and human-verified production deployment.

---

## Execution Context

@C:/Users/DannyTam-Tham/OneDrive - Tam-Tham/Projects/Corporation/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/DannyTam-Tham/OneDrive - Tam-Tham/Projects/Corporation/.opencode/get-shit-done/templates/summary.md

---

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-deployment-infrastructure/05-DEPLOYMENT-INFRASTRUCTURE-RESEARCH.md
@.planning/phases/05-deployment-infrastructure/05-01-PLAN-SUMMARY.md
@.planning/phases/05-deployment-infrastructure/05-02-PLAN-SUMMARY.md

---

## Tasks

<task type="auto">
  <name>Task 1: Create deployment verification script</name>
  <files>tests/05-deployment/verify-deployment.sh</files>
  <action>
    Create `tests/05-deployment/verify-deployment.sh` with automated verification commands:

    **Script structure:**
    - Shebang: `#!/bin/bash`
    - Exit on error: `set -e`
    - Color output for readability
    - Section headers for each requirement

    **Verification tests:**

    1. **DEPLOY-01: GitHub Actions workflow exists**
       ```bash
       echo "Checking GitHub Actions workflow..."
       test -f .github/workflows/azure-static-web-apps.yml
       gh workflow list | grep -q "azure-static-web-apps"
       ```

    2. **DEPLOY-02: Doppler integration present**
       ```bash
       echo "Checking Doppler integration..."
       grep -q "dopplerhq/action" .github/workflows/azure-static-web-apps.yml
       ```

    3. **DEPLOY-03: Action versions pinned**
       ```bash
       echo "Checking action version pinning..."
       grep -E "@v[0-9]+" .github/workflows/azure-static-web-apps.yml | wc -l
       ```

    4. **DEPLOY-04: Deploy on push to main**
       ```bash
       echo "Checking deployment trigger..."
       grep -q "push:" .github/workflows/azure-static-web-apps.yml
       grep -q "branches:" .github/workflows/azure-static-web-apps.yml
       grep -q "main" .github/workflows/azure-static-web-apps.yml
       ```

    5. **DEPLOY-05: Build command configured**
       ```bash
       echo "Checking build command..."
       grep -q "app_build_command" .github/workflows/azure-static-web-apps.yml
       grep -q "npm run build" .github/workflows/azure-static-web-apps.yml
       ```

    6. **CLOUD-01: DNS resolves to Azure SWA**
       ```bash
       echo "Checking DNS resolution..."
       dig +short tamtham.com | grep -q "azurestaticapps.net"
       ```

    7. **CLOUD-02: danny.tamtham.com accessible**
       ```bash
       echo "Checking danny.tamtham.com..."
       curl -I -s -o /dev/null -w "%{http_code}" https://danny.tamtham.com
       ```

    8. **CLOUD-03: helen.tamtham.com accessible**
       ```bash
       echo "Checking helen.tamtham.com..."
       curl -I -s -o /dev/null -w "%{http_code}" https://helen.tamtham.com
       ```

    9. **CLOUD-04: Bot Fight Mode enabled**
       ```bash
       echo "Checking Bot Fight Mode..."
       # Manual verification required - note in output
       echo "Verify in Cloudflare Dashboard -> Security -> WAF -> Bot Fight Mode"
       ```

    10. **CLOUD-05: Cache Rules configured**
        ```bash
        echo "Checking Cache Rules..."
        # Manual verification required - note in output
        echo "Verify in Cloudflare Dashboard -> Caching -> Cache Rules"
        ```

    **Output formatting:**
    - Use colored output (green for pass, red for fail)
    - Print summary at end with total pass/fail counts
  </action>
  <verify>
    <automated>chmod +x tests/05-deployment/verify-deployment.sh && bash tests/05-deployment/verify-deployment.sh 2>&1 | head -20</automated>
  </verify>
  <done>
    Verification script created with:
    - All 10 requirement checks (DEPLOY-01 to DEPLOY-05, CLOUD-01 to CLOUD-05)
    - Automated tests for workflow, Doppler, DNS, and subdomain access
    - Manual verification notes for Bot Fight Mode and Cache Rules
    - Colored output and pass/fail summary
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 verification checklist</name>
  <files>.planning/phases/05-deployment-infrastructure/WAVE0-CHECKLIST.md</files>
  <action>
    Create `.planning/phases/05-deployment-infrastructure/WAVE0-CHECKLIST.md` with pre-deployment verification checklist:

    **Section 1: Doppler Setup**
    - [ ] Doppler account created
    - [ ] Workplace "tamtham-website" created
    - [ ] Project "tamtham-website" created
    - [ ] Config "production" created
    - [ ] All secrets added (TURNSTILE_SECRET_KEY, AZURE_*)
    - [ ] Service token generated with read permissions
    - [ ] DOPPLER_TOKEN added to GitHub Secrets
    - [ ] AZURE_STATIC_WEB_APPS_API_TOKEN added to GitHub Secrets

    **Section 2: Azure Static Web Apps**
    - [ ] Azure SWA resource created in Azure Portal
    - [ ] Deployment credentials available (API token)
    - [ ] API functions directory structure correct (`api/verify-danny/`, `api/verify-helen/`)
    - [ ] function.json files present for both APIs
    - [ ] Build command tested locally (`npm run build`)

    **Section 3: Cloudflare DNS**
    - [ ] Domain added to Cloudflare
    - [ ] Nameservers updated at registrar
    - [ ] DNS propagation complete (wait 5-30 minutes)
    - [ ] CNAME record created for tamtham.com
    - [ ] Proxy enabled (orange cloud)
    - [ ] DNS verified: `dig tamtham.com`

    **Section 4: Cloudflare Tunnels**
    - [ ] Zero Trust account created in Cloudflare
    - [ ] Tunnel created for danny.tamtham.com
    - [ ] Tunnel created for helen.tamtham.com
    - [ ] cloudflared installed on home server
    - [ ] Tunnel authentication completed
    - [ ] Tunnel configuration file created
    - [ ] Tunnel service started and running
    - [ ] Tunnel status shows "Connected"

    **Section 5: Cloudflare Security**
    - [ ] Bot Fight Mode enabled
    - [ ] Cache Rules configured (not legacy Page Rules)
    - [ ] WAF rules reviewed and adjusted if needed

    **Section 6: GitHub Actions**
    - [ ] Workflow file committed to repository
    - [ ] Workflow enabled in GitHub Actions tab
    - [ ] Manual workflow run tested successfully

    **Section 7: Final Verification**
    - [ ] `tests/05-deployment/verify-deployment.sh` created
    - [ ] All checklist items completed
    - [ ] Production deployment successful
    - [ ] All subdomains accessible
  </action>
  <verify>
    <automated>test -f .planning/phases/05-deployment-infrastructure/WAVE0-CHECKLIST.md && grep -c "\- \[" .planning/phases/05-deployment-infrastructure/WAVE0-CHECKLIST.md</automated>
    Expected: At least 30 checklist items
  </verify>
  <done>
    Checklist file created with:
    - 7 sections covering all setup requirements
    - 30+ individual verification items
    - Checkbox format for easy tracking
    - Clear completion criteria
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of production deployment</name>
  <files>Production environment</files>
  <action>
    Verify complete production deployment through manual testing:

    **What to verify:**
    1. GitHub Actions workflow runs successfully on push to main
    2. Site deploys to Azure Static Web Apps
    3. Site accessible at Azure SWA staging URL
    4. Cloudflare DNS resolves correctly
    5. All subdomains accessible via Cloudflare Tunnels
    6. Bot Fight Mode active
    7. Cache Rules working (check response headers)

    **Manual tests:**
    1. Trigger deployment: `git push origin main`
    2. Monitor deployment: `gh run watch --workflow=azure-static-web-apps.yml`
    3. Check Azure Portal for deployment status
    4. Visit https://tamtham.com in browser
    5. Visit https://danny.tamtham.com in browser
    6. Visit https://helen.tamtham.com in browser
    7. Check response headers: `curl -I https://tamtham.com`
       - Look for `CF-RAY` header (Cloudflare proxy active)
       - Look for `Strict-Transport-Security` header (HSTS)
    8. Verify Bot Fight Mode: Cloudflare Dashboard -> Security -> WAF
    9. Verify Cache Rules: Cloudflare Dashboard -> Caching -> Cache Rules
  </action>
  <how-to-verify>
    1. GitHub Actions:
       - Visit: GitHub -> Actions tab
       - Verify workflow ran successfully
       - Check for Doppler setup success
       - Check for build success
       - Check for deployment success

    2. Site accessibility:
       - Open browser to https://tamtham.com
       - Verify site loads correctly
       - Verify all pages accessible
       - Verify navigation works

    3. Subdomain access:
       - Open browser to https://danny.tamtham.com
       - Verify tunnel connection (should show Cloudflare IP in headers)
       - Open browser to https://helen.tamtham.com
       - Verify tunnel connection

    4. Response headers:
       curl -I https://tamtham.com
       Expected headers:
       - Server: cloudflare
       - CF-RAY: <some-id>
       - Strict-Transport-Security: max-age=31536000; includeSubDomains

    5. Cloudflare Dashboard:
       - Bot Fight Mode: On
       - Cache Rules: Configured
       - Tunnels: Connected
  </how-to-verify>
  <resume-signal>Type "approved" when all verifications complete successfully, or describe issues</resume-signal>
</task>

</tasks>

---

## Verification

**Automated verification:**
```bash
# Run verification script
bash tests/05-deployment/verify-deployment.sh

# Check workflow status
gh run list --workflow=azure-static-web-apps.yml

# Check tunnel status
cloudflared tunnel list
```

**Manual verification:**
1. Visit all three domains in browser
2. Check Cloudflare Dashboard for status
3. Verify Azure Portal deployment logs

---

## Success Criteria

- [ ] Verification script created at `tests/05-deployment/verify-deployment.sh`
- [ ] Wave 0 checklist created at `.planning/phases/05-deployment-infrastructure/WAVE0-CHECKLIST.md`
- [ ] GitHub Actions workflow runs successfully
- [ ] Site deploys to Azure SWA
- [ ] tamtham.com resolves and loads correctly
- [ ] danny.tamtham.com accessible via tunnel
- [ ] helen.tamtham.com accessible via tunnel
- [ ] Cloudflare headers present in responses
- [ ] Bot Fight Mode enabled
- [ ] Cache Rules configured
- [ ] Human verification approved

---

## Output

After completion, create `.planning/phases/05-deployment-infrastructure/05-03-PLAN-SUMMARY.md`