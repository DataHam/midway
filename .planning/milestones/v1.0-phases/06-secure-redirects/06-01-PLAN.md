---
phase: 06-secure-redirects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/verify-danny/index.js
  - src/api/verify-helen/index.js
  - static/verify-danny.html
  - static/verify-helen.html
autonomous: true
requirements: [GATE-06]

must_haves:
  truths:
    - "User is automatically redirected to danny.tamtham.com after passing Danny's CAPTCHA"
    - "User is automatically redirected to helen.tamtham.com after passing Helen's CAPTCHA"
    - "API handles both direct navigation and Fetch-based verification with appropriate redirects"
  artifacts:
    - path: "src/api/verify-danny/index.js"
      provides: "Danny verification redirect logic"
    - path: "src/api/verify-helen/index.js"
      provides: "Helen verification redirect logic"
  key_links:
    - from: "static/verify-danny.html"
      to: "src/api/verify-danny/index.js"
      via: "Fetch POST"
---

<objective>
Implement and verify the hybrid 302 redirect logic for successful Turnstile verifications, ensuring a seamless transition from the gate pages to the protected subdomains.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Finalize Hybrid Redirects in Workers</name>
  <files>src/api/verify-danny/index.js, src/api/verify-helen/index.js</files>
  <action>
    Update both worker scripts to ensure:
    1. Consistent 302 redirect for 'text/html' Accept headers.
    2. JSON response with { redirectUrl } for AJAX/Fetch requests.
    3. Proper CORS headers (Access-Control-Allow-Origin: *) on both 200 and 302 responses if applicable.
    4. Redirect URLs point to 'https://danny.tamtham.com' and 'https://helen.tamtham.com' respectively.
  </action>
  <verify>
    curl -I -X POST -H "Accept: text/html" -d '{"token":"test"}' [worker_url]
    (Check for HTTP 302 and Location header)
  </verify>
  <done>Workers return correct redirect responses based on headers.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize Client-Side Redirect Handling</name>
  <files>static/verify-danny.html, static/verify-helen.html, pages/verify-danny.html, pages/verify-helen.html</files>
  <action>
    Ensure the submitVerification() function in all gate pages correctly handles the JSON response:
    - If data.redirectUrl is present, set window.location.href = data.redirectUrl.
    - Ensure error handling is robust if the redirect fails.
    - Sync changes between 'static/' and 'pages/' directories.
  </action>
  <verify>
    Inspect browser console during manual test to confirm window.location.href assignment.
  </verify>
  <done>Client-side JS handles API redirect instructions.</done>
</task>

</tasks>

<success_criteria>
1. Successful verification leads to the correct subdomain.
2. The user experience is seamless without manual clicks after CAPTCHA completion.
</success_criteria>