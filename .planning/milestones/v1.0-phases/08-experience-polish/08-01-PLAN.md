---
phase: 08-experience-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/index.html
  - src/css/input.css
  - scripts/optimize-images.js
  - src/api/verify-danny/function.json
  - src/api/verify-helen/function.json
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05
  - IMG-01
  - IMG-02
  - IMG-03
  - IMG-04
  - IMG-05
  - API-05
must_haves:
  truths:
    - "Home page has a hero section with HDMain.webp background and TamThamLogo.png overlay"
    - "Navigation grid has 4 cards that route to bio pages or verification gates"
    - "All images are optimized in WebP format with responsive srcset variants"
    - "Below-fold images are lazy-loaded while the hero image is eager-loaded"
    - "Azure Functions API configuration uses proper function.json and host.json files"
  artifacts:
    - path: "static/index.html"
      provides: "Home page markup"
    - path: "scripts/optimize-images.js"
      provides: "Image optimization script"
    - path: "src/api/verify-danny/function.json"
      provides: "Azure Functions config"
  key_links:
    - from: "static/index.html"
      to: "/pages/verify-danny.html"
      via: "href"
      pattern: "href=.*verify-danny"
---

<objective>
Finalize the home page layout, optimize all image assets for performance, and clean up the Azure Functions API configuration.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Home Page Layout & Metadata (HOME-01 to HOME-05)</name>
  <files>static/index.html</files>
  <action>
    Update static/index.html to ensure the hero section uses HDMain.webp as the background with TamThamLogo.png overlay.
    Verify the 4-card navigation grid routes correctly to Danny Bio, Helen Bio, and their respective CAPTCHA gates (NOT direct subdomains).
    Ensure responsive layout classes (mobile 1 col, tablet 2 cols, desktop 4 cols) are applied.
    Verify OpenGraph and Twitter meta tags exist and are correct.
  </action>
  <verify>
    grep -q "og:title" static/index.html && grep -q "verify-danny.html" static/index.html
  </verify>
  <done>Home page is fully responsive, contains correct links, and has social sharing metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Asset Optimization (IMG-01 to IMG-05)</name>
  <files>static/index.html, scripts/optimize-images.js</files>
  <action>
    Ensure `scripts/optimize-images.js` is configured to convert images to WebP and generate responsive variants (375, 768, 1024, 1920px).
    Update `static/index.html` `<img>` and background tags to use the optimized WebP images with proper `srcset`.
    Set explicit `width` and `height` attributes to prevent CLS.
    Set `loading="eager"` on the hero image and `loading="lazy"` on below-the-fold images.
  </action>
  <verify>
    grep -q "loading=\"lazy\"" static/index.html && grep -q "srcset" static/index.html
  </verify>
  <done>All images are optimized WebP formats, have srcset variants, use explicit dimensions, and are loaded with correct eager/lazy strategies.</done>
</task>

<task type="auto">
  <name>Task 3: API Configuration Cleanup (API-05)</name>
  <files>src/api/verify-danny/function.json, src/api/verify-helen/function.json</files>
  <action>
    Create or verify `function.json` for both `verify-danny` and `verify-helen` endpoints.
    Ensure bindings are correctly set to `httpTrigger` with proper methods (GET, POST) and `authLevel` is appropriately configured for Azure Functions standard.
  </action>
  <verify>
    cat src/api/verify-danny/function.json | grep -q "httpTrigger"
  </verify>
  <done>API is fully compliant with Azure Functions standards with proper function.json configurations.</done>
</task>

</tasks>

<success_criteria>
- Home page has a fully functional and responsive 4-card navigation.
- All images are optimized WebP, prevent CLS, and use correct loading attributes.
- API endpoints have correct function.json files.
</success_criteria>